{
  "name": "@deployable/socket.io-kafka",
  "version": "0.1.0",
  "description": "Kafka adapter for socket.io",
  "main": "index.js",
  "scripts": {
    "test": "istanbul cover _mocha && istanbul check-coverage",
    "release:test": "set -uex; yarn test; git push; npm version prerelase; npm publish --tag testing; git push --tags",
    "release:patch": "set -uex; rm -rf node_modules; yarn install; yarn test; git push; npm version patch; npm publish; git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/deployable/socket.io-kafka.git"
  },
  "keywords": [
    "socket.io",
    "kafka"
  ],
  "author": "Guilherme Hermeto",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/deployable/socket.io-kafka/issues"
  },
  "homepage": "https://github.com/deployable/socket.io-kafka",
  "dependencies": {
    "bluebird": "^3.3.5",
    "debug": "^2.1.3",
    "kafka-node": "~1.3.3",
    "socket.io-adapter": "~0.5.0"
  },
  "devDependencies": {
    "eslint": "^3.14.1",
    "istanbul": "^0.3.11",
    "jasmine": "^2.2.1",
    "sinon": "^1.17.7",
    "mocha": "^3.2.0"
  }
}
